#include<stdio.h>
int printNo(int x,int n){
    if(x>n) return 0;
    printf("%d\n",x);
    printNo(x+1,n);
}
int main(){
    int n;
    printf("Enter any no:");
    scanf("%d",&n);
    printNo(1,n);
    return 0;
}
